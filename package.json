{
  "name": "windows-10-portfolio-os",
  "version": "6.0.0",
  "description": "Full-Stack Windows 10 Portfolio OS built with Node.js, Express, SQLite, and Vanilla Web OS Shell",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node -e \"const { fork, spawn } = require('child_process'); const server = fork('server.js'); setTimeout(() => { const tester = spawn(process.execPath, ['--test', 'tests/suite.test.js'], { stdio: 'inherit' }); tester.on('exit', (code) => { server.kill(); process.exit(code); }); }, 3000);\""
  },

  "keywords": [
    "portfolio",
    "windows10",
    "web-os",
    "express",
    "sqlite",
    "full-stack"
  ],
  "author": "Bhavy",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "helmet": "^8.3.0",
    "sqlite3": "^6.0.1"
  }
}
