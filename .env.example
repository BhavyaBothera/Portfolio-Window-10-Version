# Server Configuration
PORT=5000
NODE_ENV=development

# Admin API Authentication Secret (Must be configured in production)
ADMIN_TOKEN=replace_with_a_long_random_secret_token

# Database Path
DB_PATH=./db/portfolio.sqlite

# CORS Allowed Origin (Set to your domain in production, e.g. https://yourportfolio.com)
CORS_ORIGIN=*
